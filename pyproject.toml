[project]
name = "neurodivergent-resource-toolkit"
version = "0.1.0"
description = "A comprehensive toolkit for enriching, categorizing, and managing neurodivergent support resources using AI-powered data extraction and categorization."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "playwright>=1.55.0",
    "openpyxl>=3.1.0",
    "python-dotenv>=1.0.0",
    "requests>=2.31.0",
]
